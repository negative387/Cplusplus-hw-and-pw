/*
Завдання 3. Що виведе наступний код? Без запуску поясніть, чому використання printf з еліпсисом може
бути небезпечним:
*/

#include <cstdio>

int main()
{
    const char* str = "Hello";
    int value = 42;

    // Помилка в типах String (потрібен %s), та у Value (потрібен %d):
    printf("String: %d, Value: %s\n", str, value);
}
//printf з еліпсисом небезпечний, бо легко можуть не збігатись типи або кількість у аргументів, а перевірки компіляції немає :(